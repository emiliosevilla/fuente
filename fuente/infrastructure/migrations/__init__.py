"""SQL migration scripts for the Vault-local job store.

Files are named `<version>_<name>.sql` and applied in ascending version
order by `fuente.infrastructure.sqlite_store.JobStore`. This package exists
so the migrations directory ships with the installed `fuente` package and
can be located by path relative to this module.
"""
