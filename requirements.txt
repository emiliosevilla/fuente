# Core production dependencies (mirrors pyproject.toml with exact pins).
# Pinning strategy: see docs/dependency-matrix.md
watchdog==6.0.0
psutil==7.2.2
minirag-hku @ git+https://github.com/HKUDS/MiniRAG.git@e204d239421f45004852953679927fdf6733f236
json-repair==0.63.3
tiktoken==0.14.0
nltk==3.10.3
rouge==1.0.1
scikit-learn==1.9.0
nano-vectordb==0.0.4.3
pipmaster==1.1.13
networkx==3.6.1
requests==2.34.2
pydantic==2.13.4
pyyaml==6.0.3
python-docx==1.2.0
pdfplumber==0.11.10
openpyxl==3.1.5
python-pptx==1.0.2
extract-msg==0.56.0
pysqlite3-binary>=0.5.4; platform_system == "Linux"

# Optional feature sets — install via extras instead of listing here:
#   pip install -e ".[webview]"   # PyWebView GUI console
#   pip install -e ".[audio]"     # faster-whisper transcription
#   pip install -e ".[ocr]"       # Tesseract OCR (Python bindings + Pillow)
#   pip install -e ".[office]"    # MarkItDown + Docling converters
#   pip install -e ".[dev]"       # PyInstaller packaging
#   pip install -e ".[test]"      # pytest
#   pip install -e ".[all]"       # full desktop stack (webview+audio+ocr+office+rag)
